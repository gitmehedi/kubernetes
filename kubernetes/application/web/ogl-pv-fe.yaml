apiVersion: v1
kind: PersistentVolume
metadata:
   name: nginx-config
   labels:
      type: ng-config
spec:
   storageClassName: manual
   capacity:
       storage: 2Gi
   accessModes:
       - ReadWriteOnce
   hostPath:
       path: '/mnt/data'
       
